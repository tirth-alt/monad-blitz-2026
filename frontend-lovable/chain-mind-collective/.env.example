# ChainMind frontend environment

# Wallet mode.
#   false (or unset) → built-in MOCK wallet (safe default for demos)
#   true             → real wallet via MetaMask (wagmi) on Monad testnet
VITE_USE_REAL_WALLET=false
